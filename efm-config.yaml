version: 2
tools:
  vim:
    lint-command: 'vint -'
    lint-stdin: true

  markdown-markdownlint:
    lint-command: 'markdownlint -s'
    lint-stdin: true
    lint-formats:
      - '%f:%l %m'
      - '%f:%l%c %m'
      - '%f: %l: %m'
